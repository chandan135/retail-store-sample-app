apiVersion: v1
kind: Service
metadata:
    name: catalog-mysql
    labels: 
      app.kubernetes.io/name: catalog
      app.kubernetes.io/instance: catalog
      app.kubernetes.io/component: mysql
      app.kubernetes.io/owner: retail-store-sample
spec:
    clusterIP: None # Headless svc
    selector:
      app.kubernetes.io/name: catalog
      app.kubernetes.io/instance: catalog
      app.kubernetes.io/component: mysql
      app.kubernetes.io/owner: retail-store-sample
    ports:
    - name: mysql
      port: 3306
      targetPort: mysql

# Headless svc : It allows each pod to be reached using stable DNS name